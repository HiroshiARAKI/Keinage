# OpenWeather City List

`city.list.json.gz` is a gzip-compressed copy of OpenWeather's sample city
list:

https://bulk.openweathermap.org/sample/city.list.json.gz

Keinage reads this file on the server for country and city selection. It is
included in standalone builds and is not imported into the client bundle.
